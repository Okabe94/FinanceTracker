package com.software.financetracker.ui.theme

import androidx.compose.ui.graphics.Color

// Four-tier green brand system — each mapped to a distinct surface role
val StarbucksGreen = Color(0xFF006241)  // brand signal, h1 headings, section headers
val GreenAccent = Color(0xFF00754A)     // primary filled CTAs, FAB
val HouseGreen = Color(0xFF1E3932)      // deep bg, footer-level and feature-band surfaces
val GreenUplift = Color(0xFF2B5148)     // secondary dark surfaces, decorative accents
val GreenLight = Color(0xFFD4E9E2)      // form-valid tints, light utility surfaces

// Gold — ceremony / rewards moments only; never general-purpose
val Gold = Color(0xFFCBA258)
val GoldLight = Color(0xFFDFC49D)
val GoldLightest = Color(0xFFFAF6EE)

// Surfaces
val White = Color(0xFFFFFFFF)
val NeutralWarm = Color(0xFFF2F0EB)     // warm cream primary page canvas
val Ceramic = Color(0xFFEDEBE9)         // zone separators, soft section washes
val NeutralCool = Color(0xFFF9F9F9)     // dropdown / form-card surfaces
val RewardsGreen = Color(0xFF33433D)    // muted slate-green for reward surface text

// Text
val TextOnLight = Color(0xDE000000)     // 87% black — primary on light surfaces
val TextOnLightSoft = Color(0x94000000) // 58% black — secondary/metadata on light
val TextOnDark = Color(0xFFFFFFFF)      // primary on dark-green surfaces
val TextOnDarkSoft = Color(0xB3FFFFFF)  // 70% white — secondary on dark surfaces

// Semantic
val ErrorRed = Color(0xFFC82014)
val WarningYellow = Color(0xFFFBBC05)
